import type { DtrConfig, GlobalConfig } from "../types";

export const globalConfigStaterData: GlobalConfig = {
  allFiles: [],
  allTemplates: [],
};

export const dtrConfigStaterData: DtrConfig = {
  codeFolder: "",
  addedCode: [],
};
